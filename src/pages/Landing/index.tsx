import { Box, Button, Input } from "@chakra-ui/react";
import React from "react";

const Landing = () => {
  return (
    <Box>
      <section>
        <Input />
      </section>
      <Button>Hello</Button>
    </Box>
  );
};

export default Landing;
